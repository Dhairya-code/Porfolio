/// <reference types="react" />

// Ensure the React JSX namespace is available to the TypeScript compiler.
// This helps projects where the automatic inclusion of `@types/react` is prevented
// by a restrictive `types` compiler option.
